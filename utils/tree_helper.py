class TreeHelper(object):
    def search_parent(self, tree, child, layer = 1, prev_parent = 'root'):
        for k, v in list(tree.items):
            if(k == child):
                return prev_parent
            else:
                if len(v) >= 1:
                    layer += 1
                
                else :
                    if len(v) >= 1:
                        layer += 1
                        found =sear


    def search_path(tree, node):
        for k, v in list(tree.items()):
            if(k == child):

            else:
                
        